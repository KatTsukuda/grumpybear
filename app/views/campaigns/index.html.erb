<h1>Campaigns</h1>

<div class="campaigns-list">
  <% @campaigns.each do |cmpgn| %>
   <h2><%= link_to cmpgn.campaign_title, campaign_path(cmpgn) %></h2>
    <p>
      <%= image_tag(cmpgn.image_url.to_s, width: '300px') %>
      Category: <%= cmpgn.category %>
    </p>
  <% end %>
</div>
